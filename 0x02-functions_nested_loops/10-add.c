#include "main.h"

/**
 * add - adds two integers
 * @x: integers to be added to y
 * @y: integer to be added to x
 *
 * Return: the sum of the two
 */
int add(int x, int y)
{
	int m = x + y;

	return (m);
}
